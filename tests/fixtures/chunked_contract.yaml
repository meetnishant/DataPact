contract:
  name: chunked_demo
  version: 2.0.0
dataset:
  name: chunked_demo
fields:
  - name: id
    type: integer
    rules:
      unique: true
  - name: score
    type: integer
    rules:
      max: 10
