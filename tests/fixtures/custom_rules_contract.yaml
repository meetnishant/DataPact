contract:
  name: custom_rules_contract
  version: 2.0.0
dataset:
  name: custom_rules
fields:
  - name: score
    type: float
    required: true
    rules:
      custom:
        field_max_value:
          value: 10
custom_rules:
  - name: dataset_min_rows
    config:
      value: 3
    severity: WARN
